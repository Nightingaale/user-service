spring:
  config:
    import: vault://secret/prod/user-service
  cloud:
    vault:
      uri: http://vault:8200
      authentication: token
      token: root
      kv:
        enabled: true
        backend: secret
        application-name: prod/user-service
      enabled: true